name: Custom GitHub Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run a sample command
        run: echo "Hello, world!"

      - name: 22f1001747@ds.study.iitm.ac.in
        run: echo "This is a test action for 22f1001747@ds.study.iitm.ac.in"
